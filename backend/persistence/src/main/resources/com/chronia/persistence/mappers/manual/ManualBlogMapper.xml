<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chronia.persistence.mappers.manual.ManualBlogMapper">
    <select id="getBlogsWithPagination" resultType="com.chronia.persistence.models.manual.ManualBlog">
        select
            blogs.content,
            blogs.id,
            blogs.title,
            users.username,
            users.profile_photo_url as profilePhotoUrl
        from blogs
        join users
            on users.uid = blogs.uid
        where blogs.uid = #{userId}
        order by blogs.id
        limit #{pageSize} offset #{offset}
    </select>
</mapper>
